version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Run backend tests
    task:
      jobs:
        - name: Build and run tests
          commands:
            - checkout
            - docker-compose -f docker-compose.backend.test.yml -p ci up
